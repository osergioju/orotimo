# Servidor
PORT=4000

# Autenticação (preparado para JWT em etapa futura — não utilizado ainda)
JWT_SECRET=change-me
JWT_EXPIRES_IN=1d

# Banco de dados — PostgreSQL (conceitual nesta etapa, sem conexão real ainda)
DATABASE_URL=postgresql://user:password@localhost:5432/scale_engine
