# Dependências reais do bridge.py (Node -> Python). O requirements.txt
# original é do projeto Django legado inteiro (mathwork/) e traz pacotes que
# o bridge nunca importa (Django, gunicorn, whitenoise, mysqlclient,
# django-cors-headers, sqlparse) — alguns nem instalam em Python 3.12.
numpy==1.26.4
pandas==2.2.1
PuLP==2.7.0
PyMySQL==1.1.0
python-dotenv==1.0.1
tabulate==0.9.0
